{
  "name": "api",
  "private": true,
  "version": "0.0.0",
  "description": "Mock backend for the React/Angular examples (json-server)",
  "scripts": {
    "dev": "json-server --watch db.json --port 3001"
  },
  "devDependencies": {
    "json-server": "^0.17.4"
  }
}
